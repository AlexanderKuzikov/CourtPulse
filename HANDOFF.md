# CourtPulse — HANDOFF

> Создан: 2026-08-12. Новая сессия: прочитай AGENTS.md, docs/CONTEXT.md, затем продолжай.

## Статус

Код **написан полностью** (ядро + дашборд + документация), но **не отлажен**:

- ✅ `src/` — probe (DNS→TCP→TLS→HTTP), scheduler (волны 15 мин, concurrency 2, jitter), storage (JSONL+state+incidents), api (node:http), index
- ✅ `public/` — дашборд (vanilla JS + uPlot локально)
- ✅ `data/courts.json` — из CourtDesk (10 287 судов, регион 59 → 181 сайт)
- ✅ docs/, README, AGENTS, package.json (zero-dep, TS через `--experimental-strip-types`)
- ⚠️ `npm run once` **завис**: волна 181 суда × таймауты до 30с при concurrency 2 = до 45 минут. Успело 16 проб (в `data/probes/2026-08-12.jsonl`), процесс убит.

## Что делать дальше (по приоритету)

1. **Отладить волну**: добавить `--limit N` (проба первых N судов) для быстрой проверки. Проверить: DNS_OK, статусы OK/BANNED, запись JSONL, state.json, детект инцидентов.
2. **Прогнать полную волну** (ночью, ~45 мин) и убедиться в скорости/корректности.
3. **Проверить дашборд**: `npm start` → http://127.0.0.1:8781 — KPI, график 7 дней, таблица, инциденты, модалка суда.
4. **Регион**: в `src/config.ts` `region: '59'` — при желании расширить.

## Известные грабли

- Node ≥22.6: импорты в src — **с `.ts` расширением** (`from './config.ts'`), НЕ `.js` (type-stripping не резолвит .js).
- WAF ГАС банит за rate-limit: НЕ повышать concurrency >2, НЕ снижать интервал <15 мин.
- Капча на главной не появляется (только на поиске/деле) — статус 200 = OK.
- Инцидент: ≥3 проб подряд неудач; `incidentsThreshold` в config.ts.

## Контекст создания

Идея: статистика доступности судов (после WAF-банов в CourtDesk). Обсуждено: новый репо, интервал 15 мин, капча не мешает. Решения — в docs/DECISIONS.md (ADR-001 zero-dep, ADR-002 вежливая волна).
