# CourtPulse — CONTEXT

> Последнее обновление: 2026-08-12

## Статус
| Компонент | Статус | Заметка |
|-----------|--------|---------|
| Ядро | ✅ отлажено | **баг: в httpGet не было `req.end()`** — запрос не отправлялся, каждая проба ждала 30с таймаут. Исправлено: проба ~300мс |
| Дашборд | ✅ проверен | /api/summary, /api/courts (181), /api/history, статика uPlot — 200 |
| Данные | ✅ | Полная волна: 181/181 OK за 222с (limit=8: 8/8 OK за 31с) |
| Запуск | ✅ | Мониторинг работает в фоне (интервал 15 мин, порт 8781) |
| Репо | ⏳ | локальный, gh repo create не выполнен |

## Open-проблемы
| # | Priority | Описание |
|---|----------|----------|
| 1 | low | Капча на главной не ожидается (только на поиске/деле) — если появится, status=OK всё равно (200) |
| 2 | low | redirects (301/302) → сейчас HTTP_ERR; можно следовать 1 редиректу (в пробах встречались 301, напр. motovil.perm.sudrf.ru) |
| 3 | low | Старые TIMEOUT-пробы от бага req.end() (22 шт, 2026-08-12) тянут ok7/uptime24h вниз — уйдут сами по мере накопления данных |

## Журнал работ
| Дата | Изменение |
|------|-----------|
| 2026-08-12 | Идея: статистика доступности судов (после WAF-банов в CourtDesk). Написано ядро + дашборд + документация |
| 2026-08-12 | Отладка: добавлен `--limit N` (index.ts, scheduler.ts); найден баг `req.end()` в probe.ts:httpGet — пробы всегда TIMEOUT 30с, волна "висела" 45 мин. Фикс: проба ~300мс, полная волна 181/181 OK за 222с. Очищены ложные инциденты (probes=1 от бага). Мониторинг запущен в фоне |

## Структура
```
см. AGENTS.md
```
