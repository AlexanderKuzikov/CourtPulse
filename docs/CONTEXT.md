# CourtPulse — CONTEXT

> Последнее обновление: 2026-08-12

## Статус
| Компонент | Статус | Заметка |
|-----------|--------|---------|
| Ядро | ✅ отлажено | баг req.end() исправлен; **ревью Qwen 3.8 Max применено** (фиксы #1-3, С1-С8, Н1-Н13) |
| Дашборд | ✅ проверен | /api/summary, /api/courts (181), /api/history, статика uPlot — 200 |
| Данные | ✅ | Полная волна: 181/181 OK за 222с (limit=8: 8/8 OK за 31с) |
| Запуск | ✅ | Мониторинг работает в фоне (интервал 15 мин, порт 8781) |
| Репо | ✅ | https://github.com/AlexanderKuzikov/CourtPulse (main) |

## Open-проблемы
| # | Priority | Описание |
|---|----------|----------|
| 1 | med | **BANNED-семантика не решена**: сейчас BANNED считается неудачей (ложные инциденты + заниженный uptime). Qwen рекомендует: для uptime «жив», для инцидентов — предохранитель 6 BANNED подряд. Ждёт решения пользователя |
| 2 | med | **Concurrency 3-4**: пер-хост рейт не меняется (1 запрос/суд/15 мин), агрегейт ×1.5-2 — безопасно для WAF. Ждёт решения пользователя (AGENTS.md запрещает менять без обсуждения) |
| 3 | low | Retention .jsonl (~3.5 МБ/день → ~105 МБ/мес) — удалять старше 90-180 дней? Ждёт решения |
| 4 | low | Старые TIMEOUT-пробы от бага req.end() (22 шт, 2026-08-12) тянут ok7/uptime24h вниз — уйдут сами |
| 5 | low | Тесты из ревью Qwen (12 шт, node:test) не написаны — в qwen38-review-result.md |

## Журнал работ
| Дата | Изменение |
|------|-----------|
| 2026-08-12 | Идея: статистика доступности судов (после WAF-банов в CourtDesk). Написано ядро + дашборд + документация |
| 2026-08-12 | Отладка: добавлен `--limit N`; найден баг `req.end()` в probe.ts — пробы всегда TIMEOUT, волна «висела» 45 мин. Фикс: проба ~300мс, полная волна 181/181 OK за 222с. Мониторинг запущен в фоне |
| 2026-08-12 | **Ревью Qwen 3.8 Max применено** (qwen38-review-result.md): #1 try/finally + .catch в once (running не залипает); #2 streak-счётчик инцидентов (порог 3 честно); #3 абсолютный HTTP-дедлайн + destroy (нет утечки сокетов) + классификация ошибок (ECONNRESET≠TIMEOUT); С1 таймауты dns 10→5с, http 30→20с; С3 agent:false; С4 атомарная запись tmp+rename; С5 кэш API TTL 45с + инвалидация по волне; С7 path traversal через resolve+sep; С8 Fisher-Yates; Н1-Н13 (MIME, days clamp, medianMs только OK, --limit обе формы, getIncidents(), server.on('error'), понятная ошибка без courts.json, AGENTS.md .ts). Проверено: --once --limit=3 2× — 3/3 OK ~30с, state.json сохраняется по ходу |
| 2026-08-13 | Дашборд: пустая модалка поверх всего экрана — `.modal { display:flex }` перебивал атрибут `hidden` (авторский CSS сильнее UA `[hidden]{display:none}`); фикс `.modal[hidden]{display:none}`. Баг uPlot: `series.value(v => …)` — первый аргумент это self-uPlot, не значение → `v.toFixed` падал при отрисовке легенды/курсора; фикс `(u, v) => …`. Светлая тема (палитра CSS-переменных + цвета графиков). `Cache-Control: max-age=300` → `no-cache` для статики (браузер держал старые файлы до 5 мин). favicon `data:,` (убрал 404). Убраны капсы: `text-transform: uppercase` из `.kpi .l`, `.panel h2`, `th` (заголовки/подписи в нормальном регистре). Проверено через Playwright (DOM): модалка hidden, светлый фон, капсы убраны, KPI 8, строк 181 |
| 2026-08-13 | Дашборд+ядро (вторая серия): `th` всегда по центру (style.css: `th { text-align:center }`, td — left). Uptime-полоска+% в одну строку (`.up-cell`: inline-flex, nowrap) — не переносится ни при 100%, ни при 96%. Ось времени графика: uPlot с `time:true` ждёт секунды, код отдавал миллисекунды → подписи «58570» (год 58565); фикс `ts.map(t => t/1000)` (и в модальном графике `p.ts/1000`). Сортировка судов по приоритету в courts.ts: Областной → Арбитражный → Районный → Мировой (TYPE_PRIORITY), внутри — по коду. У мировых и районных срезан суффикс « Пермского края» в названии. Проверено: node-проверка loadRegionCourts (181: 1+1+34+145, имена чистые), Playwright на свежем кэше (chart7: секунды, 06–13.08; th центр; up-cell nowrap; модалка none). Требует рестарта сервера: courts.ts-изменения + Cache-Control no-cache |
| 2026-08-13 | Таблица судов: клиентская сортировка по всем 8 колонкам (клик: asc → desc → сброс к дефолту). Дефолт — недоступные сверху (severity: down=0, UNKNOWN=1, BANNED=2, OK=3), внутри группы — приоритет типов. Числовые колонки: пропуски всегда в конце (для Отклика/Uptime/Последней пробы 0 = нет данных; для Падений/Проб 0 = валидное значение). Названия — localeCompare ru с numeric (Участок № 2 < № 10). Индикатор ▲/▼. Баг модального графика: hooks.drawSeries передавался функцией, uPlot требует массив → крэш `yi[e].forEach`; фикс `drawSeries: [...]`. Проверено на мок-сервере (Playwright): дефолтный порядок, все циклы кликов, фильтры/поиск/модалка |
| 2026-08-13 | KPI-карточки: цифры скакали при переносе подписи («Судов в мониторинге» — 2 строки, сосед — 1). Фикс: `.kpi` → flex column, `.kpi > .l` фикс-высота 2.8em (2 строки) + центрирование. Проверено: все 8 значений на одной линии (top 125px) |

## Структура
```
см. AGENTS.md
```
